// Layer 3 — amarre envelope. v1 is empty: every wire record IS the Layer 4 payload.
// See docs/PROTOCOL.md §5.1.

export type AmarreVersion = '1.0.0';
export const AMARRE_VERSION: AmarreVersion = '1.0.0';
